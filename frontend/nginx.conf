server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # React Router owns the URLs, so every path has to return index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
