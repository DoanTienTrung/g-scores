[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router owns the URLs. Without this, refreshing on /tra-cuu asks
# Netlify for a file that does not exist and returns 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
